export type TaskType = {
    id: string;
    todo: string;
    completed: boolean;
}
export type NotificationMessageType = {
    id:number;
    text: string;
}
export type DirtyCheckButtonType = {
    isDisabled: boolean;
}